<%= form_with(model: @board_generator, url: creators_path) do |form|%>
  <% if @board_generator.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(@board_generator.errors.count, "error") %> prohibited this creator from being saved:</h2>

        <ul>
          <% @board_generator.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
  <% end %>

   <div>
      <%= form.label :email, style: "display: block" %>
      <%= form.text_field :email %>
    </div>

    <div>
      <%= form.label :name, style: "display: block" %>
      <%= form.text_field :name %>
    </div>

    <div>
      <%= form.label :height, style: "display: block" %>
      <%= form.number_field :height%>
    </div>

    <div>
      <%= form.label :width, style: "display: block" %>
      <%= form.number_field :width %>
    </div>

    <div>
      <%= form.label :number_of_mines, style: "display: block" %>
      <%= form.number_field :number_of_mines%>
    </div>

    <br/>
    <div>
      <%= form.submit "Generate Board"%>
    </div>
<% end%>